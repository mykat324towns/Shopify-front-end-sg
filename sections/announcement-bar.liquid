{% if section.settings.text != blank %}
  <div
    id="announcement-bar"
    class="announcement-bar"
    data-section-id="{{ section.id }}"
  >
    <div class="container announcement-bar__inner">
      <p class="announcement-bar__text">{{ section.settings.text }}</p>
      {% if section.settings.dismissible %}
        <button class="announcement-bar__dismiss" aria-label="Dismiss announcement">&#x2715;</button>
      {% endif %}
    </div>
  </div>
{% endif %}

{% schema %}
{
  "name": "Announcement Bar",
  "settings": [
    {
      "type": "text",
      "id": "text",
      "label": "Announcement text",
      "info": "Keep to one line — wrapping eats fold real estate on mobile. Rotate weekly. Suggested options: \"Free U.S. shipping over $100\" / \"Hand-poured from authentic bottles\" / Real low-stock alerts only: \"Last 18ml left from the Le Beau bottle.\"",
      "default": "Free U.S. shipping over $100"
    },
    {
      "type": "checkbox",
      "id": "dismissible",
      "label": "Allow customers to dismiss",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Announcement Bar"
    }
  ]
}
{% endschema %}
